# CR match: river + bridges

- River uses a canvas water texture (bright CR blue with white
  light streaks) that scrolls slowly forever (waterTex offset
  driven in render()).
- Bridge decks use a plank canvas texture (seams + nail dots);
  rails gained end posts.
