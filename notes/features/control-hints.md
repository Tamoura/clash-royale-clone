# Control hints

- Each hand card shows its 1-4 keyboard shortcut as a corner chip
  (re-added on every hand rebuild in hud.update).
- Right-click on the field cancels the current selection (Escape
  already did).
