# CR match: elixir bar

- Purple droplet counter (matching the card cost badges) now leads
  the bar; hud.ts reordered the row.
- Fill is a vertical purple gradient with a white top highlight;
  ten segment ticks are drawn with a repeating-gradient overlay on
  the trough so the fill slides underneath.
- Verified in browser.
