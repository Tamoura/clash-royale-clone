# CR match: HP pills

Reference /tmp/cr-ref-3.jpg: tower HP is a rounded colored pill with
the number inside and a level badge at the end.

- makeHpBar: rounded dark trough (shared canvas pill texture),
  inset colored fill with a gloss highlight that scales with it.
- Tower bars: numeric HP moved inside the pill, circular team-color
  level badge ("9") capping the left end.
- setHpFill offset adjusted for the inset fill width.
