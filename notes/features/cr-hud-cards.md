# CR match: HUD card frames

- Cards: gold/orange borders, royal-blue vertical gradient, inner
  highlight + drop shadow, dark-outlined names, hover/selected lift
  with a golden glow; locked cards desaturate instead of fading.
- Elixir cost badge moved to the top-left as a purple droplet
  (circle + CSS triangle tip), like CR.
- HUD panel: navy gradient with a raised top border; next-card slot
  gets a framed gradient well.
- Verified in browser.
