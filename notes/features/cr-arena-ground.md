# CR match: arena ground

- Playfield is now one slab with a CR-style checkerboard grass top:
  a canvas texture with one pixel per tile (NearestFilter for crisp
  edges), two greens like the training arena.
- Pale stone edging rails around the playfield with chunky corner
  posts, replacing the bare slab edge.
- Verified in-browser: checker + edging + crew read clearly.
