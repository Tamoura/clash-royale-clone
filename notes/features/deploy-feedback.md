# Invalid-deploy feedback

- tryDeployAt now consults checkDeploy: "no-elixir" shakes the
  elixir row, "bad-spot" shakes the hand row, both with a flat
  denial buzz (throttled). CSS errShake keyframes; hud.flashError
  restarts the animation via offsetWidth reflow.
