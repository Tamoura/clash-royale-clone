{
  "name": "clash-royale-clone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 3101 --strictPort",
    "build": "tsc && vite build",
    "preview": "vite preview --port 3101 --strictPort",
    "test": "vitest run",
    "test:watch": "vitest",
    "relay": "node --experimental-transform-types server/relay.ts",
    "play": "node scripts/play-lan.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^25.1.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@types/three": "^0.184.1",
    "three": "^0.184.0",
    "ws": "^8.21.0"
  }
}
